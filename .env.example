# WeRead Agent API key (https://weread.qq.com)
WEREAD_API_KEY=wrk-your-key-here

# Optional: full path to node (needed if `node` is not in PATH for daily auto-sync)
# Run `which node` in terminal, then paste the path here.
# NODE_BIN=/opt/anaconda3/bin/node

# Optional overrides (defaults match app.js)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
# WEREAD_HIGHLIGHTS_PER_BOOK=3
