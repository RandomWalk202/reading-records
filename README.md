# 阅读记录

一个本地运行的阅读记录网站，支持：

- 添加书籍
- 修改书籍
- 删除书籍
- 写读书笔记
- 上传封面

数据保存在浏览器的 `localStorage` 中，不需要后端服务。

## 使用

直接用浏览器打开 `index.html` 即可。

如果想通过本地服务器访问，也可以运行：

```bash
python3 -m http.server 4173
```

然后打开 `http://localhost:4173`。
